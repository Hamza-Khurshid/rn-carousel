import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    buttonsContainer: {
        marginTop: 15,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-around',
    }
})